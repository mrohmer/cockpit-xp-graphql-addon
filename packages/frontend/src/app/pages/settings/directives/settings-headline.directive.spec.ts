import { SettingsHeadlineDirective } from './settings-headline.directive';

describe('SettingsHeadlineDirective', () => {
  it('should create an instance', () => {
    const directive = new SettingsHeadlineDirective();
    expect(directive).toBeTruthy();
  });
});
