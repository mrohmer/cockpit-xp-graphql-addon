import { CellPercentageDirective } from './cell-percentage.directive';

describe('CellPercentageDirective', () => {
  it('should create an instance', () => {
    const directive = new CellPercentageDirective();
    expect(directive).toBeTruthy();
  });
});
