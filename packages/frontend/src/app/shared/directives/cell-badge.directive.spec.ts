import { CellBadgeDirective } from './cell-badge.directive';

describe('CellBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new CellBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
