import { CellSuperLineDirective } from './cell-super-line.directive';

describe('CellSuperLineDirective', () => {
  it('should create an instance', () => {
    const directive = new CellSuperLineDirective();
    expect(directive).toBeTruthy();
  });
});
