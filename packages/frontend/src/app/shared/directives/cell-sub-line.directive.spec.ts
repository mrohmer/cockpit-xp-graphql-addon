import { CellSubLineDirective } from './cell-sub-line.directive';

describe('CellSubLineDirective', () => {
  it('should create an instance', () => {
    const directive = new CellSubLineDirective();
    expect(directive).toBeTruthy();
  });
});
